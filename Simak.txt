#Masukkan Langkah Pertama

 $apt update && apt upgrade
 $apt install python
 $apt install php
 $apt install bash
 $apt install git
 $git clone https://github.com/KU74N9/TembakKutang.git
 $cd TembakKutang
 $chmod +x *
 $sh IsiKuota.sh

#MenuPilihan
